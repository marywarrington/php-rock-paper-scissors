<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
  </head>
  <body>
    <div class="container">
        <h1>Rock Paper Scissors</h1>
        <h3><em>BOOM!</em></h3>
        {% if results is not empty %}
            {% for result in results %}
            <h3>{{ result }} wins!</h3>
            {% endfor %}
        {% endif %}
    </div>
    <div class="container">
        <form action='/'>
            <button type='submit' class='btn btn-primary' name='submit' value='submit'>Play Again!</button>
        </form>
    </div>
  </body>
</html>
